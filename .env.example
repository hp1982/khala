# 供 prisma CLI 使用（migrate dev / db push），使用文件型嵌入式 SQLite，迁移可持久化
DATABASE_URL="file:./prisma/dev.db"

# 运行时数据库：开发测试阶段使用内存 SQLite（:memory:），进程内自包含、无需外部服务
APP_DATABASE_URL=":memory:"

# 初始超级管理员（启动时若不存在 role=1 管理员则自动创建）
ADMIN_INIT_USERNAME="admin"
ADMIN_INIT_PASSWORD="admin123"